## Fragzilla originally made by Florin_TK on [discord](https://discord.com/channels/488444879836413975/1223940533844705320)

```
{"workspaces":{"Fragment Automation":[["fragments_lib","\n; space for convenience\n:import TurboExec\n\n; name of the package\n#package(name) WS.Fragzilla:{name}\n\n\n; enter idle mode or anvil mode\n; \"IA\" = idle or anvil = 0 or 1\n#mode \"<size=0>Mode\"\n\n\n\n\n"],["Init","\n;space for convenience\n:import fragments_lib\n\n:name {package(Init)}\n\n#whatopen(building) if(isopen({building}), {building}, \"\")\n#shutdown \\\n\t{whatopen(\"towertesting\")} \\\n\t. {whatopen(\"tradingpost\")} \\\n\t. {whatopen(\"powerplant\")} \\\n\t. {whatopen(\"factory\")} \\\n\t. {whatopen(\"laboratory\")} \\\n\t. {whatopen(\"shipyard\")} \\\n\t. {whatopen(\"workshop\")} \\\n\t. {whatopen(\"arcade\")} \\\n\t. {whatopen(\"museum\")} \\\n\t. {whatopen(\"headquarters\")} \\\n\t. {whatopen(\"constructionfirm\")} \\\n\t. {whatopen(\"statueofcubos\")} \\\n\t. {whatopen(\"mine\")}\n\n\n:global string variables\n:global bool running\n:local int keeper\n\nkey.f()\n\ngotoif(exit, running)\nshow({shutdown}, false)\nexit()\nexecutesync(\"{turbo}:start\")\ngis({mode}, if(variables == \"\", -1, gig({mode})))\nrunning = true\ngss(variables, \"cooldown=\" . d2s(1.1) . \",\" . \"hammercount=\" . i2s(1) . \"</size=0\" . \">\")\nexecutesync(\"{turbo}:stop\")\nexecutesync(\"{package(Floor Detection)}\")\nexecute(\"{package(Farm)}\")\nexecute(\"{package(Hammer)}\")\ngoto(99)\nexit:\nrunning = false\n\n;space for convenience\n"],["useless","\n; space for convenience\n:import fragments_lib\n\n:name {package(Floor Detection)}\n:local bool floor\n; program to detect if is floor 2 or floor 1\n\ngotoif(99, gig({mode}) != -1)\n{click.relative(580.0/800.0, 40.0/450.0, 0.5, 0.5)}  ;open settings\n{click.relative(470.0/800.0, 365.0/450.0, 0.5, 0.5)} ;enter graphics\n\nwait(0.5)\n;let things load\n\nscrollbar({pos.relative(705.0/800.0, 85.0/450.0, 0.80, 0.5)}, -1.0, 0.0) ;scroll down to see UI level\nslider({pos.relative(370.0/800.0, 145.0/450.0, 0.5, 0.5)}, 1.0) ;set UI at 100%\n\n;you're now done with settings\nshow(\"towertesting\", true)\n\n{click.relative(162.0/800.0, 97.0/450.0, 0.0, 1.0)} ;launch round\nwaitwhile(health(true) > 0.5 && enemies() > 50)\nexit()\nwait(0.5) ;wait for everything to load up\n\nshow(\"workshop\", true) ;enter workshop\n\n{click.relative(215.0/800.0, 35.0/450.0, 0.0, 0.0)}  ;click on floor 2\n{click.relative(148.0/800.0, 325.0/450.0, 0.5, 0.5)} ;ensure it's infinity tab sellected\n{click.relative(380.0/800.0, 47.0/450.0, 0.0, 0.0)} ;enter anvil mode if not already\n\n\n"],["Farm","\n; space for convenience\n:import fragments_lib\n\n:name {package(Farm)}\n\n:global string variables\n:global bool running\n\ngotoif(if(running, 1, 99), gig({mode}) == 1)\nshow(\"workshop\", false)\nshow(\"towertesting\", true)\n{click.relative(90.0/800.0, 35.0/450.0, 0.0, 0.0)}\nwait(s2d(sub(variables, 9, index(variables, \",\", 9) - 9), 3.0))\ngis({mode}, 1)\ngotoif(1, running)"],["Hammer","\n; space for convenience\n:import fragments_lib\n\n:name {package(Hammer)}\n\n:global string variables\n:global bool running\n:local int remClicks\n\n\ngotoif(if(running, 1, 99), gig({mode}) != 1)\nshow(\"towertesting\", false)\nshow(\"workshop\", true)\nremClicks = s2i(sub(variables, index(variables, \"hammercount=\", 26), index(variables, \"</\", 26) - index(variables, \"hammercount=\", 26)), 3) + 1\nhammertime:\nremClicks = remClicks - 1\n{click.relative(720.0/800.0, 40.0/450.0, 0.5, 0.5)}\ngotoif(hammertime, remClicks > 0)\ngis({mode}, 0)\ngotoif(0, running)"]]}}
```
