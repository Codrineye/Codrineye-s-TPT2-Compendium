:import RNG_manip lib
:budget_cap 100

:global int predicted_rng
:global int actual_rng

key.t()

goto(rng.line_2)
{rng.line_1}
{rng.line_2}
{rng.line_3(guess)}

guess:
{rng.next}
predicted_rng = {rng.get_range(0, 1)}
actual_rng = rnd(0, 1)
