# Clicker
A project made by discord user [megahakerek](https://github.com/Megahackerboss36/tpt-2-clicker/blob/main/README.md)
```
{"workspaces":{"clicker":[["Shop Contents",":import Shop Creation\n\n;gss(\"this long story...\", \" +10 cpc, 15 cps - MT10.. this no final! cost: 10k\")\n\n{add_item(noob clicker, 1, you start this clicker!, 50)}\n{add_item(cylindro clicker, 3, this easiest boss!, 350)}\n{add_item(chaos complete, 5, this hardest region!, 1e3)}\n{add_item(first era, 7, WOW! mt8!, 3e3)}\n{add_item(this long story..., 10)}\n{add_page}\n{add_page}\n\n:const string items '{lua(return shop.concat_items())}'\n:const string pages '{lua(return shop.concat_pages())}'\n\n:const int items_count {lua(return #shop.item)}\n\n:const int name_displace {lua(return shop.text.max_name_size)}\n:const int description_displace {lua(return shop.text.max_description_size)}\n\n:const int segment {lua(return shop.max_length)}\n\n"],["Shop Creation","{lua(\\\n  shop = {};\\\n  shop.item = {};\\\n  shop.max_length = 0;\\\n  shop.text = {};\\\n  shop.text.max_name_size = 0;\\\n  shop.text.max_description_size = 0;\\\n  shop.text.text_ending_marker = [[;]];\\\n  shop.precision = {};\\\n  shop.precision.price = 5;\\\n  shop.precision.effect = 2;\\\n  shop.page = {};\\\n  shop.max_page_size = 0;\\\n\\\n  local function x_digit_precision(fmt, digits, input)\\\n    local number = tostring(input):gsub(\" \", \"\");\\\n    return string.sub(\\\n      string.format(fmt .. digits .. \"f\", number), \\\n      1, \\\n      digits + 2\\\n    );\\\n  end\\\n  local function text_formatting(text)\\\n    return text:gsub(\"^ +\", \"\"):gsub(\" +$\", \"\") .. \\\n      shop.text.text_ending_marker;\\\n  end\\\n\\\n  function shop.add_item(price, effect_idle, effect_click, name, description)\\\n    local shop_index = 1 + #shop.item;\\\n    local new_item = {};\\\n    new_item.price = x_digit_precision(\\\n      \"%.\",\\\n      shop.precision.price,\\\n      price\\\n    );\\\n    new_item.effect = {};\\\n    new_item.effect.idle = x_digit_precision(\\\n      \"%.\",\\\n      shop.precision.effect,\\\n      effect\\\n    );\\\n    new_item.name = text_formatting(name);\\\n    new_item.description = text_formatting(description);\\\n    shop.text.max_name_size = math.max(\\\n      shop.text.max_name_size,\\\n      #new_item.name\\\n    );\\\n    shop.text.max_description_size = math.max(\\\n      shop.text.max_description_size,\\\n      #new_item.description\\\n    );\\\n    shop.max_length = shop.precision.price + 2 + shop.precision.effect + 2\\\n      + shop.text.max_name_size + shop.text.max_description_size;\\\n    shop.item[shop_index] = new_item;\\\n    return [[:const int item]] .. shop_index .. [[ ]] .. shop_index;\\\n  end\\\n\\\n  function shop.concat_items()\\\n    local function repetition_string(big, small)\\\n      local space = big - small;\\\n      return string.rep('.', space);\\\n    end\\\n    local shop_items = #shop.item;\\\n    local items_concat = {};\\\n    for i = 1, shop_items do\\\n      items_concat[1 + #items_concat] = shop.item[i].effect;\\\n      items_concat[1 + #items_concat] = shop.item[i].price;\\\n      items_concat[1 + #items_concat] = shop.item[i].name;\\\n      items_concat[1 + #items_concat] = repetition_string(\\\n        shop.text.max_name_size,\\\n        #shop.item[i].name\\\n      );\\\n      items_concat[1 + #items_concat] = shop.item[i].description;\\\n      items_concat[1 + #items_concat] = repetition_string(\\\n        shop.text.max_description_size,\\\n        #shop.item[i].description\\\n      );\\\n    end\\\n    return table.concat(items_concat);\\\n  end\\\n\\\n  function shop.add_page()\\\n    local page_index = 1 + #shop.page;\\\n    local new_page = {};\\\n    if shop.page[page_index - 1] == nil then\\\n      new_page.count = #shop.item;\\\n    else\\\n      new_page.count = #shop.item - shop.page[page_index - 1].count;\\\n    end\\\n    new_page.size = new_page.count // 10;\\\n    shop.max_page_size = math.max(\\\n      shop.max_page_size,\\\n      new_page.size\\\n    );\\\n    shop.page[page_index] = new_page;\\\n    return [[:const int page]] .. page_index .. [[ ]] .. page_index;\\\n  end\\\n\\\n  function shop.concat_pages()\\\n    local page_count = #shop.page;\\\n    local page_fitter = shop.max_page_size;\\\n    local pages_concat = {};\\\n    for i = 1, page_count do\\\n      pages_concat[1 + #pages_concat] = x_digit_precision(\\\n        \"%0\",\\\n        shop.max_page_size,\\\n        shop.page[i].count\\\n      );\\\n    end\\\n    return table.concat(pages_concat);\\\n  end\\\n)}\n\n#add_item(name, effect, description, price) {lua(return shop.add_item(\\\n  [[{price}]],\\\n  [[{effect}]],\\\n  [[{name}]],\\\n  [[{description}]]\\\n))}\n\n#add_page {lua(return shop.add_page())}\n\n"],["clicker lib","#package(name) clicker:{name}\n\n#identifier \"clicker\"\n"],["clicker(start)",":import clicker lib\n:name {package(clicker(start))}\n\n:global int page\n:global int cps\n:global int cpc\n:global int click\n:global int chose\n\nkey.c()\n\nchose = 1\nclick = 1\ncpc = 1\ncps = 0\npage = 1\ngss(\"=======shop\", \"======\")\nexecute(\"{package(1)}\")\nexecute(\"{package(page system)}\")\nexecute(\"{package(cps)}\")\n"],["1",":import clicker lib\n:name {package(1)}\n\nexecute(\"{package(unset shop)}\")\nwait(0.5)\nexecute(\"{package(unset shop)}\")\nwait(0.5)\ngss(\"noob clicker\", \" +1 cpc - you start this clicker! cost:50\")\nwait(0.05)\ngss(\"cylindro clicker\", \" +3 cpc - this easiest boss! cost:350\")\nwait(0.05)\ngss(\"chaos complete\", \" +5 cps - this hardest region! cost: 1k\")\nwait(0.05)\ngss(\"first era\", \" +7 cpc - WOW! mt8! cost: 3k\")\nwait(0.05)\ngss(\"this long story...\", \" +10 cpc, 15 cps - MT10.. this no final! cost: 10k\")\nwait(0.05)\n"],["2",":import clicker lib\n:name {package(2)}\n\n:global string pyramido\n\nexecute(\"{package(unset shop)}\")\nwait(0.5)\nexecute(\"{package(unset shop)}\")\nwait(0.5)\npyramido = \" +25 cpc - this easy! cost:25k\"\nwait(0.05)\ngss(\"maxing out the lab\", \" +50 cps - you maxing labolatory!  cost:40k\")\nwait(0.05)\ngss(\"infinity board\", \" +80 cpc - what is it... you have MT11! cost:85k\")\nwait(0.05)\ngss(\"cubos jr.\", \" +60 cpc, 60cps - hmm new boss cost: 120k\")\nwait(0.05)\ngss(\"facitily ai\", \" +100 cpc, 100 cps - required to win! cost: 250k\")\nwait(0.05)\n"],["3",":import clicker lib\n:name {package(3)}\n\n:global string challanger\n\nexecute(\"{package(unset shop)}\")\nwait(0.5)\nexecute(\"{package(unset shop)}\")\nwait(0.5)\ngss(\"first infinity...\", \" +300 cpc - wow, infinity... this end? cost:1M\")\nwait(0.05)\nchallanger = \" +500 cps, 500 cpc - huh, challange tutorial use!  cost:4M\"\nwait(0.05)\ngss(\"ifn anvil\", \" +3.5k cpc - you upgrade afk in anvil cost:13M\")\nwait(0.05)\ngss(\"tower customization\", \" +9k cpc - flex multipy you click cost: 25M\")\nwait(0.05)\ngss(\"farm gem\", \" +10k cpc, 10k cps - shipydard, trading post... cost: 100M\")\nwait(0.05)\n"],["page system decrement",":import clicker lib\n:name {package(page system)}\n\n:global int page\n\nkey.a()\n\ngotoif(99, page <= 1)\npage = page - 1\nexecute(\"{package()}\" . i2s(page))\n"],["page system increment",":import clicker lib\n:name {package(page system)}\n\n:global int page\n\nkey.d()\n\ngotoif(99, page >= 5)\npage = page + 1\nexecute(\"{package()}\" . i2s(page))\n"],["system buyed offer",":import clicker lib\n:name {package(system buyed offer)}\n\n:global int cpc\n\n#click.int.get global.int.get(\"click\")\n#click.int.set(value) global.int.set(\"click\", {value})\n\n#click.double global.double.get(\"click\")\n\ngotoif(skip, ({click.int.get} > 49) && contains(impulse(), \"key.1\"))\ngotoif(0, ({click.double} > 349.0) && contains(impulse(), \"2\"))\ngotoif(0, ({click.double} > 999.0) && contains(impulse(), \"3\"))\ngotoif(0, ({click.double} > 2999.0) && contains(impulse(), \"4\"))\ngotoif(0, ({click.double} > 9999.0) && contains(impulse(), \"5\"))\n\nend:\ngoto(1)\n\nskip:\n{click.int.set({click.int.get} - 50)}\ncpc = cpc + 1\ngoto(end)\n"],["click system",":import clicker lib\n:name {package(click system)}\n\n:global int cpc\n:global int click\n\nmouse.0.down()\nkey.0()\n\nclick = click + cpc"],["cps",":import clicker lib\n:name {package(cps)}\n\n\n:global int cps\n:global int click\n\nwait(1.0)\nclick = click + cps\nexecute(\"{package(cps)}\")\n"],["buyed better posibble of 1",":import clicker lib\n:name {package(buyed better posibble of)}\n\n:global int cps\n:global int cpc\n:global int click\n\nkey.b()\n\ngotoif(buy5, click >= 10000)\ngotoif(buy4, click >= 3000)\ngotoif(buy3, click >= 1000)\ngotoif(buy2, click >= 350)\ngotoif(buy1, click >= 50)\n\n\nreturn:\ngoto(end)\n\nbuy1:\nclick = click - 50\ncpc = cpc + 1\ngoto(return)\n\nbuy2:\nclick = click - 350\ncpc = cpc + 3\ngoto(return)\n\nbuy3:\nclick = click - 999\ncps = cps + 5\ngoto(return)\n\nbuy4:\nclick = click - 3000\ncpc = cpc + 7\ngoto(return)\n\nbuy5:\nclick = click - 10000\ncpc = cpc + 10\ncps = cps + 15\ngoto(return)\n\nend:\n"],["buyed better posibble of 2",":import clicker lib\n:name {package(buyed better posibble of)}\n\n\n:global int cps\n:global int cpc\n:global int click\n\nkey.b()\n\ngotoif(buy5, click >= 250000)\ngotoif(buy4, click >= 120000)\ngotoif(buy3, click >= 85000)\ngotoif(buy2, click >= 40000)\ngotoif(buy1, click >= 25000)\n\nreturn:\ngoto(exit)\n\nbuy1:\nclick = click - 25000\ncpc = cpc + 1\ngoto(return)\n\nbuy2:\nclick = click - 40000\ncps = cps + 50\ngoto(return)\n\nbuy3:\nclick = click - 84999\ncps = cpc + 80\ngoto(return)\n\nbuy4:\nclick = click - 120000\ncpc = cpc + 60\ncps = cps + 80\ngoto(return)\n\nbuy5:\nclick = click - 250000\ncpc = cpc + 100\ncps = cps + 100\ngoto(return)\n\nexit:\n"],["buyed better posibble of 3",":import clicker lib\n:name {package(buyed better posibble of)}\n\n:global int cps\n:global int cpc\n:global int click\n\nkey.b()\n\ngotoif(buy1, click >= 100000000)\ngotoif(buy2, click >= 25000000)\ngotoif(buy3, click >= 13000000)\ngotoif(buy4, click >= 4000000)\ngotoif(buy5, click >= 1000000)\n\nreturn:\ngoto(exit)\n\nbuy1:\nclick = click - 1000000\ncpc = cpc + 300\ngoto(return)\n\nbuy2:\nclick = click - 4000000\ncps = cps + 500\ncpc = cpc + 500\ngoto(return)\n\nbuy3:\nclick = click - 13000000\ncpc = cpc + 3500\ngoto(return)\n\nbuy4:\nclick = click - 25000000\ncpc = cpc + 9000\ngoto(return)\n\nbuy5:\nclick = click - 100000000\ncpc = cpc + 10000\ncps = cps + 10000\ngoto(return)\n\nexit:"],["unset shop",":import clicker lib\n:name {package(unset shop)}\n\ngu(\"noob clicker\")\ngu(\"cylindro clicker\")\ngu(\"chaos complete\")\ngu(\"first era\")\ngu(\"this long story...\")\ngu(\"pyramido\")\ngu(\"maxing out the lab\")\ngu(\"infinity board\")\ngu(\"cubos jr.\")\ngu(\"facitily ai\")\ngu(\"first infinity...\")\ngu(\"challanger\")\ngu(\"ifn anvil\")\ngu(\"tower customization\")\ngu(\"farm gem\")"],["chose buyed offer 2",":import clicker lib\n:name {package(chose buyed offer)}\n\n:global int cps\n:global int cpc\n:global int click\n:global int chose\n\nkey.s()\n\ngotoif(buy1, (chose == 6) && (click >= 25000))\ngotoif(buy2, (chose == 7) && (click >= 40000))\ngotoif(buy3, (chose == 8) && (click >= 85000))\ngotoif(buy4, (chose == 9) && (click >= 120000))\ngotoif(buy5, (chose == 10) && (click >= 250000))\n\ngoto(exit)\n\nbuy1:\nclick = click - 25000\ncpc = cpc + 25\ngoto(exit)\n\nbuy2:\nclick = click - 40000\ncps = cps + 50\ngoto(exit)\n\nbuy3:\nclick = click - 85000\ncps = cps + 80\ngoto(exit)\n\nbuy4:\nclick = click - 120000\ncpc = cpc + 60\ncps = cps + 60\ngoto(exit)\n\nbuy5:\nclick = click - 250000\ncpc = cpc + 100\ncps = cps + 100\ngoto(exit)\n\nexit:"],["chose system select decrement",":import clicker lib\n:name {package(chose system select)}\n\n:global int chose\n\nkey.z()\n\ngotoif(99, chose <= 1)\nchose = chose - 1\n"],["chose system select increment",":import clicker lib\n:name {package(chose system select)}\n\n:global int chose\n\nkey.x()\n\ngotoif(99, chose >= 25)\nchose = chose + 1\n"],["chose buyed offer 1",":import clicker lib\n:name {package(chose buyed offer)}\n\n:global int cps\n:global int cpc\n:global int click\n:global int chose\n\nkey.s()\n\ngotoif(buy1, (chose == 1) && (click > 49))\ngotoif(buy2, (chose == 2) && (click > 349))\ngotoif(buy3, (chose == 3) && (click > 1000))\ngotoif(buy4, (chose == 4) && (click > 3000))\ngotoif(buy5, (chose == 5) && (click > 10000))\ngoto(exit)\n\nbuy1:\nclick = click - 50\ncpc = cpc + 1\ngoto(exit)\n\nbuy2:\nclick = click - 350\ncpc = cpc + 3\ngoto(exit)\n\nbuy3:\nclick = click - 1000\ncps = cps + 5\ngoto(exit)\n\nbuy4:\nclick = click - 3000\ncpc = cpc + 7\ngoto(exit)\n\nbuy5:\nclick = click - 10000\ncpc = cpc + 10\ncps = cps + 15\ngoto(exit)\n\nexit:\n"],["chose buyed offer 3",":import clicker lib\n:name {package(chose buyed offer)}\n\n:global int cps\n:global int cpc\n:global int click\n:global int chose\n\nkey.s()\n\n\ngotoif(buy1, (chose == 11) && (click >= 1000000))\ngotoif(buy2, (chose == 12) && (click >= 4000000))\ngotoif(buy3, (chose == 13) && (click >= 13000000))\ngotoif(buy4, (chose == 14) && (click >= 25000000))\ngotoif(buy5, (chose == 15) && (click >= 100000000))\n\ngoto(exit)\n\nbuy1:\nclick = click - 1000000\ncpc = cpc + 300\ngoto(exit)\n\nbuy2:\nclick = click - 4000000\ncps = cps + 500\ncpc = cpc + 500\ngoto(exit)\n\nbuy3:\nclick = click - 13000000\ncps = cps + 3500\ngoto(exit)\n\nbuy4:\nclick = click - 25000000\ncpc = cpc + 9000\ngoto(exit)\n\nbuy5:\nclick = click - 100000000\ncpc = cpc + 10000\ncps = cps + 10000\n\ngoto(exit)\n\nexit:"]]}}
```