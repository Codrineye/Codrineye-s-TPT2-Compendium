:import winAI_lib
:name {package(3spam)}

useinstant(active.index("barrier.temporal"))

stop("{package(inf)}")
execute("{package(inf)}")

execute("{spell_compactor_package(main)}")

useinstant(active.index("crate.rex"))

waituntil({timer.get} >= 2.0)

spam:
wait(0.2)
useinstant(active.index("spell.response.neutral"))
goto(spam)
